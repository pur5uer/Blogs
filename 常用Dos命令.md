常用的Dos命令

```bash
#切换盘符(到D盘) D:
#查看当前目录下所有文件 dir
#切换目录(本盘符) cd 当前盘符下目录地址
#切换目录(跨盘符) cd /d 目录地址
#清理屏幕 cls (clear screen)
#查看ip ipconfig
#打开应用 calc(计算器) notepad(记事本) mspaint(画图)
#创建目录 md 目录名
#创建文件 cd>文件名
#删除文件 del 文件名
#删除目录 rd 目录名
```

